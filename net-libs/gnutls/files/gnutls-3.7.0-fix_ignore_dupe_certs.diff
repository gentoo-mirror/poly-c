--- gnutls28-3.7.0.orig/lib/x509/common.c
+++ gnutls28-3.7.0/lib/x509/common.c
@@ -1758,6 +1758,9 @@ unsigned int _gnutls_sort_clist(gnutls_x
 	 * increasing DEFAULT_MAX_VERIFY_DEPTH.
 	 */
 	for (i = 0; i < clist_size; i++) {
+		if (gnutls_x509_crt_check_issuer(clist[i], clist[i]))
+				continue;
+
 		for (j = 1; j < clist_size; j++) {
 			if (i == j)
 				continue;
